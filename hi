local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")
local Camera = workspace.CurrentCamera
local lp = Players.LocalPlayer

-- STABILITÄTS-CHECK
if CoreGui:FindFirstChild("DeltaFinalMaster") then 
    CoreGui.DeltaFinalMaster:Destroy() 
end

local masterGui = Instance.new("ScreenGui", CoreGui)
masterGui.Name = "DeltaFinalMaster"
masterGui.ResetOnSpawn = false

-- LEAVE BUTTON
local leaveButton = Instance.new("TextButton", masterGui)
leaveButton.Size = UDim2.new(0, 100, 0, 40)
leaveButton.Position = UDim2.new(1, -120, 0, 20)
leaveButton.BackgroundColor3 = Color3.fromRGB(180, 0, 0)
leaveButton.Text = "LEAVE"
leaveButton.TextColor3 = Color3.new(1, 1, 1)
leaveButton.Font = Enum.Font.GothamBold
leaveButton.TextSize = 14
Instance.new("UICorner", leaveButton).CornerRadius = UDim.new(0, 8)
leaveButton.MouseButton1Click:Connect(function() game:Shutdown() end)

-- FPS COUNTER
local fpsLabel = Instance.new("TextLabel", masterGui)
fpsLabel.Size = UDim2.new(0, 90, 0, 30)
fpsLabel.Position = UDim2.new(0.5, -45, 0, 10)
fpsLabel.BackgroundTransparency = 0.6
fpsLabel.BackgroundColor3 = Color3.new(0, 0, 0)
fpsLabel.TextColor3 = Color3.new(0, 1, 0)
fpsLabel.Font = Enum.Font.Code
fpsLabel.TextSize = 15
local frameCount = 0
task.spawn(function()
    while task.wait(1) do fpsLabel.Text = "FPS: " .. frameCount frameCount = 0 end
end)
RunService.RenderStepped:Connect(function() frameCount = frameCount + 1 end)

-- AUTO HEADLESS & KORBLOX
task.spawn(function()
    while task.wait(0.5) do
        local char = lp.Character
        if char then
            if char:FindFirstChild("Head") then 
                char.Head.Transparency = 1 
                if char.Head:FindFirstChild("face") then char.Head.face.Transparency = 1 end
            end
            local parts = {"RightUpperLeg", "RightLowerLeg", "RightFoot", "Right Leg"}
            for _, p in pairs(parts) do if char:FindFirstChild(p) then char[p]:Destroy() end end
        end
    end
end)

-- ULTRA FPS BOOSTER (Wieder extrem wie vorher)
local function ultraBoost()
    settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
    Lighting.GlobalShadows = false
    Lighting.FogEnd = 9e9
    Lighting.Brightness = 2
    
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("Part") or v:IsA("MeshPart") or v:IsA("UnionOperation") then
            v.Material = Enum.Material.SmoothPlastic
            v.Reflectance = 0
        elseif v:IsA("Decal") or v:IsA("Texture") or v:IsA("ParticleEmitter") or v:IsA("Trail") then
            v:Destroy()
        elseif v:IsA("Explosion") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
            v:Destroy()
        end
    end
end
ultraBoost()

-- ESP MIT TRACERN & HAND-INFO (Max 8 Spieler)
local function createESP(p)
    if p == lp then return end
    local tracer = Drawing.new("Line")
    local info = Drawing.new("Text")
    
    local connection
    connection = RunService.RenderStepped:Connect(function()
        -- Nur aktiv wenn 8 oder weniger Spieler im Server
        if #Players:GetPlayers() <= 8 then
            if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = p.Character.HumanoidRootPart
                local screenPos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
                
                if onScreen then
                    -- Hand Item Check
                    local tool = p.Character:FindFirstChildOfClass("Tool")
                    local toolName = tool and tool.Name or "Empty"
                    
                    tracer.Visible = true
                    tracer.From = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y)
                    tracer.To = Vector2.new(screenPos.X, screenPos.Y)
                    tracer.Color = Color3.new(1, 1, 1)
                    tracer.Thickness = 1
                    
                    info.Visible = true
                    info.Text = p.Name .. "\nHand: " .. toolName
                    info.Position = Vector2.new(screenPos.X, screenPos.Y - 50)
                    info.Color = Color3.new(1, 1, 1)
                    info.Center = true
                    info.Outline = true
                    info.Size = 14
                else
                    tracer.Visible = false
                    info.Visible = false
                end
            else
                tracer.Visible = false
                info.Visible = false
            end
        else
            tracer.Visible = false
            info.Visible = false
        end
    end)
    
    p.AncestryChanged:Connect(function()
        if not p:IsDescendantOf(game) then
            tracer:Remove()
            info:Remove()
            connection:Disconnect()
        end
    end)
end

for _, p in pairs(Players:GetPlayers()) do createESP(p) end
Players.PlayerAdded:Connect(createESP)

print("Delta Final Master (Classic Booster + Hand-Info) geladen!")
